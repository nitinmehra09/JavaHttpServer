# javaHttpServer
A custom HTTP server built from scratch using core Java (ServerSocket &amp; Socket) without any frameworks. Handles HTTP requests, routing, and static responses to demonstrate how web servers work internally.
